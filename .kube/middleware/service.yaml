apiVersion: v1
kind: Service
metadata:
  name: middleware-service
  namespace: inference-engine
spec:
  type: ClusterIP
  selector:
    app: middleware-gateway
  ports:
  - port: 80
    targetPort: 8080
