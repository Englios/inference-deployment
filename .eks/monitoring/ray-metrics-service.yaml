apiVersion: v1
kind: Service
metadata:
  name: ray-head-metrics
  namespace: inference-engine
  labels:
    app: ray-head-metrics
spec:
  selector:
    ray.io/node-type: head
  ports:
  - name: dashboard
    port: 8265
    targetPort: 8265
