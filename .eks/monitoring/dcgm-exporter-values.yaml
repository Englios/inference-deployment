serviceMonitor:
  enabled: true
  interval: 30s

nodeSelector:
  accelerator: nvidia-gpu
  workload: inference

tolerations:
- key: dedicated
  operator: Equal
  value: inference
  effect: NoSchedule

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
